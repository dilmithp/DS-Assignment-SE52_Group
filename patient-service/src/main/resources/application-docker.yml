spring:
  cloud:
    consul:
      host: consul
  datasource:
    url: jdbc:postgresql://postgres-patient:5432/patientdb
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://keycloak:8080/realms/healthcare/protocol/openid-connect/certs
