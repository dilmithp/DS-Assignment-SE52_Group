spring:
  cloud:
    consul:
      host: consul
  datasource:
    url: jdbc:postgresql://postgres-doctor:5432/doctordb
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://65.2.171.207:8180/realms/healthcare/protocol/openid-connect/certs
